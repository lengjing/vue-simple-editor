import { Component, Vue, Prop } from 'vue-property-decorator';
import { namespace } from 'vuex-class';
import { IBrick } from '@/common/brick';

const Page = namespace('page');

export default class APostion extends Vue {
  render() {
    return (
      <div>

      </div>
    );
  }
}
